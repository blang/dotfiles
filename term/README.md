Xdefaults without proper preprocessor
=====================================

Since my system is lacking support for Xdefaults preprocessing,
i use `cpp` to preprocess manually.

```
cpp ~/.Xdefaults.pre > ~/.Xdefaults
```
